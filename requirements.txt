# Only needed for chat.py (the standalone API smoke test).
# proxy.py uses the standard library exclusively.
openai>=1.30
rich>=13              # streaming Markdown rendering + syntax highlighting
prompt_toolkit>=3.0   # slash-command autocomplete with live filtering + arrow-key navigation
# Optional: install for MCP (Model Context Protocol) stdio server support
# mcp>=1.0             # MCP support — pip install mcp
